import Link from "next/link";
import { defaultRunTimesLondon } from "@/lib/config";
import { dashboardMetrics, loadProjects, sectionLabels } from "@/lib/projects";
import { financingInstitutions, monitoredRegions } from "@/lib/taxonomy";
export default async function Home() { const projects = await loadProjects(); const metrics = dashboardMetrics(projects); const topProjects = [...projects].sort((a,b)=>b.score-a.score).slice(0,5); return <main className="shell"><div className="hero"><div><p className="eyebrow">内部管理员系统</p><h1>全球国际工程招标预警系统</h1><p>默认中文展示，按英国时间 {defaultRunTimesLondon.join("、")} 自动扫描真实官方数据源、AI 核验、去重、翻译和推送。</p></div><Link className="button" href="/admin">进入后台管理</Link></div><section className="grid metrics" aria-label="首页仪表盘指标">{metrics.map(m=><article className={`card ${m.tone??"normal"}`} key={m.label}><span>{m.label}</span><strong>{m.value}</strong></article>)}</section><section className="section-nav"><Link href="/projects/prequalification">资格预审</Link><Link href="/projects/tender">正式招标</Link><Link href="/projects/pipeline">前瞻项目</Link></section><section className="card"><h2>AI 重点推荐项目</h2>{topProjects.length===0?<p className="empty">暂无已核验项目</p>:<table className="table"><thead><tr><th>评分</th><th>板块</th><th>中文项目名称</th><th>国家</th><th>金额</th><th>状态</th><th>中国企业</th></tr></thead><tbody>{topProjects.map(p=><tr key={p.id}><td>{p.stars}</td><td>{sectionLabels[p.section]}</td><td><Link href={`/projects/${p.section}/${p.id}`}>{p.titleZh}</Link></td><td>{p.country}</td><td>{p.amountUsd?`$${(p.amountUsd/1_000_000).toFixed(0)}M`:"待核实"}</td><td>{p.status}</td><td>{p.chinaParticipation}</td></tr>)}</tbody></table>}</section><section className="grid"><div className="card"><h2>监控区域</h2><p>{monitoredRegions.map(i=>`${i.region}（${i.countries.length}国）`).join("、")}</p></div><div className="card"><h2>重点融资机构</h2><p>{financingInstitutions.join("、")}</p></div></section></main>; }
