import { redirect } from "next/navigation";
import { adminConfigStatus, isAdminSession } from "@/lib/admin-auth";
import { amountThresholdsUsd, defaultRunTimesLondon } from "@/lib/config";
import { officialSourceAdapters } from "@/lib/official-sources";
const modules=["Dashboard","项目管理","收藏","重点项目","已联系业主","已购买标书","已报名资格预审","已投标","已放弃","已授标","项目完成","待人工核实","数据源管理","人工审核","重新抓取","链接检查","运行日志","错误日志","AI日志","推送日志","系统设置","管理员设置","金额门槛设置","国家管理","区域管理","行业管理","用户权限"];
export default async function AdminPage(){if(!(await isAdminSession())) redirect("/admin/login"); const status=adminConfigStatus();return <main className="shell"><h1>后台管理</h1><p>服务端密钥仅在服务端读取，不会暴露到浏览器端。未配置的通知或 AI 能力会安全跳过并显示“未配置”。</p><section className="grid">{Object.entries(status).map(([k,v])=><div className={`card ${v?"success":"warning"}`} key={k}><span>{k}</span><strong>{v?"已配置":"未配置"}</strong></div>)}</section><div className="grid">{modules.map(x=><div className="card" key={x}>{x}</div>)}</div><section className="card"><h2>真实官方数据源</h2><ul>{officialSourceAdapters.map(s=><li key={s.key}><strong>{s.agencyName}</strong>：{s.crawlEntry}；状态：{s.enabled?"启用":"停用"}；类型：{s.kind}</li>)}</ul><p>ADB、AfDB、IsDB、EBRD 采用可配置官方 JSON/API 入口；未配置时记录失败并显示“未配置”，不会伪造抓取成功或生成虚假项目。</p></section><p>默认运行时间（英国）：{defaultRunTimesLondon.join("、")}；金额门槛：{amountThresholdsUsd.map(v=>`$${v/1_000_000}M`).join("、")}，加勒比地区自动按 $4M 执行。</p></main>}
